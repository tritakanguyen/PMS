# Backend Service Configuration (Create as Web Service)
backend:
  name: pms-backend
  type: web
  runtime: node
  buildCommand: cd database && npm install && cd ../server && npm install
  startCommand: cd server && npm start
  env:
    - NODE_ENV=production
    - PORT=5000
    - MONGODB_URI=mongodb+srv://admin:JXZWEMU6z4w1SvIJ@podmanagement.yv8dt9t.mongodb.net/?retryWrites=true&w=majority&appName=podManagement
    - ALLOWED_ORIGINS=https://pms-eqvv.onrender.com

# Frontend Service Configuration (Create as Static Site)
frontend:
  name: pms-frontend
  type: static
  buildCommand: cd client && npm install && npm run build
  publishDirectory: client/build
  env:
    - REACT_APP_API_URL=https://pms-backend-60nq.onrender.com
    - NODE_ENV=production
    - GENERATE_SOURCEMAP=false
